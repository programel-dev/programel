framework:
    http_client:
        scoped_clients:
            telegram.client:
                base_uri: 'https://api.telegram.org/bot%env(TELEGRAM_BOT_TOKEN)%/'
                timeout: 10
                max_duration: 15
                headers:
                    Accept: 'application/json'
