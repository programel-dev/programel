api_platform:
    title: Programel API
    version: 1.0.0
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
        route_prefix: /api/v1
    formats:
        jsonld: ['application/ld+json']
        json: ['application/json']
