{
  "attribution": {
    "commit": ""
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "docker compose -f docker-compose.dev.yml exec -T api vendor/bin/php-cs-fixer fix --dry-run --diff 2>/dev/null; RC=$?; if [ \"$RC\" -eq 8 ]; then printf '{\"continue\":false,\"stopReason\":\"php-cs-fixer found code style issues. Run: docker compose -f docker-compose.dev.yml exec api vendor/bin/php-cs-fixer fix\"}'; fi",
            "timeout": 30,
            "statusMessage": "Checking PHP code style..."
          }
        ]
      }
    ]
  }
}
